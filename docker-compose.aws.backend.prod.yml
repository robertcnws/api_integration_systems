services:
  aws_backend_app:
    build:
      context: ./backend_app
      dockerfile: Dockerfile.aws.backend.prod
    container_name: aws_backend_app
    restart: unless-stopped
    volumes:
      - ./backend_app:/app
      - ./backend_app/gunicorn.conf.py:/etc/nginx/gunicorn.conf.py
      - ./backend_app/static:/app/staticfiles
    ports:
      - "8000:8000"
    networks:
      - net_integration

networks:
  net_integration:
    driver: bridge